# Disposable Codex Hook Smoke Subject

This directory exists only for prompts lab 006a.

The real hook configuration is inside this disposable repo:

- `.codex/hooks.json`
- `.codex/hooks/real_hook_probe.py`

Run Codex from this directory with `--dangerously-bypass-hook-trust` only for
this smoke test. Do not copy this hook config into the parent project.
